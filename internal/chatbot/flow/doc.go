// Package flow hosts version helpers and (future) the graph engine extract (Phases 9 & 15).
// Runtime graph execution remains in handlers until a later cutover; versioning is additive.
package flow
